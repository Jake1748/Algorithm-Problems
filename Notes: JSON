JSON is a standard script format
It stands for Javascript Object Notation
It is essentially a Javascript object that was multiple properties on it
This makes it an easy way for developers to read and parse the information
JSON was edited so that it was more bare-bones and compatible for other systmes that use different programming languages.
Most languages have something similar to a JS Object which makes JSON more accessible
It is a lightweight object that functions as a very popular standard in many data transfers.
