.NET is a framework used to build and support applications. 
It accepts a few programming languages, but the most popular one is C#.
One could view .NET as something similar to and IDE, but .NET has many built-in libraries.
It is most popular to enterprises because it is bundled with the microsoft package. As a result, it is not open-sourced.
